<% provide(:title, "#{@table}") %>
  <h1><%= @table %></h1>
<table class="table">
  <thead>
  <% @row.each do |r|%>
    <th>
      <%= "#{r.name}" %>
    </th>

    <% end %>
  </thead>
<tbody>

  <tr>
    <td></td>
    <td></td>
  </tr>

<tbody>
<%= link_to "Add content", new_content_path(table_name: @table)%>
</table>
